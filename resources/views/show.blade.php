@extends('layouts.app')

@section('content')

 {{--   @if($list->items)
        @foreach($list->items as $item)
        
            @include('gift', ['gift' => $item->gift])

        @endforeach
    @endif --}}

@endsection